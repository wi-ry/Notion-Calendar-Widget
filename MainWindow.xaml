<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="NotionCalendarWidget.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        Title="Notion Calendar Widget"
        Width="800"
        Height="600"
        WindowStartupLocation="Manual"
        WindowStyle="None"
        AllowsTransparency="False"
        Background="White"
        Topmost="True"
        ResizeMode="CanResize"
        ShowInTaskbar="False"
        SourceInitialized="Window_SourceInitialized">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="40" ResizeBorderThickness="0" GlassFrameThickness="0" UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>
    <Window.Resources>
        <Style x:Key="TitleBarButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Width" Value="35" />
            <Setter Property="Height" Value="35" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#f15550" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <DockPanel LastChildFill="True">
        <!-- Custom Black Title Bar -->
        <Grid DockPanel.Dock="Top" Height="40" Background="#1a1a1a">
            <!-- Title Text on Left -->
            <TextBlock Text="Notion Calendar" Foreground="White" FontSize="14" FontWeight="Bold" VerticalAlignment="Center" Margin="15,0,0,0" IsHitTestVisible="False" />
            
            <!-- Buttons on Right (NonClientArea=False so they receive clicks) -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,5,0">
                <!-- Refresh Button -->
                <Button Click="RefreshButton_Click" Style="{StaticResource TitleBarButton}">
                    <TextBlock Text="&#x21BA;" FontSize="18" Foreground="White" />
                </Button>
                <!-- Close Button -->
                <Button Click="CloseButton_Click" Style="{StaticResource TitleBarButton}">
                    <TextBlock Text="&#x2715;" FontSize="16" Foreground="White" />
                </Button>
            </StackPanel>
        </Grid>

        <!-- WebView2 Container -->
        <Border x:Name="WebViewContainer" Background="White" />
    </DockPanel>
</Window>
